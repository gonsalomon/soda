  - schema:
      name: Any schema changes
      {{ alert_type }}:
        when schema changes:
          - column delete
          - column add
          - column index change
          - column type change
